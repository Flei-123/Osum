Format: https://www.debian.org/doc/packaging-manuals/copyright-format/1.0/
Upstream-Name: Osum
Source: https://github.com/Flei-123/Osum

# WHY THIS FILE EXISTS
# Most source files carry an `SPDX-License-Identifier:` line, and that line is
# authoritative for the file it stands in. This file assigns a licence to the
# rest by path -- binary assets, and any file whose line 1 has to stay what it
# is -- so that every single file in the repository has an answer.
# The reasoning is in LICENSING.md; the full texts are in LICENSE
# (GPL-2.0-only) and LICENSE.MIT.

Files: *
Copyright: 2026 Justin (Flei123)
License: GPL-2.0-only

Files:
 lib/libc/*
 kernel/user/crt.s
 kernel/user/user.ld
 kernel/user/ulib.fi
 kernel/user/tools.fi
 kernel/user/wlib.fi
 kernel/user/wlibc.fi
 kernel/user/appdir.fi
 kernel/user/nidx.fi
 kernel/user/pw.fi
 kernel/user/flate.fi
Copyright: 2026 Justin (Flei123)
Comment: THE RING 3 LIBRARIES -- everything a program links against. The line
 is mechanical and can be rechecked: a Ring 3 program defines `u_start`, a
 Ring 3 library does not. On main that is 70 programs and 8 libraries, plus
 crt.s, user.ld and lib/libc.
License: MIT

Files: assets/osum-mono.ttf assets/osum-sans.ttf
Copyright: Bitstream Inc. and the DejaVu authors
Comment: GLYPH OUTLINES CUT OUT OF DejaVu Sans Mono and DejaVu Sans by
 tools/ttf/subset.py, reproducible octet for octet (tools/wm/run.sh lines
 219-226). The same holds for the 1520 octets of 8x16 bitmaps inside
 kernel/font.fi, rasterised from DejaVu Sans Mono -- see the header of that
 file, lines 7 to 13. The Bitstream Vera / DejaVu licence is permissive and
 GPL-2.0-compatible, so this is not a licence conflict. IT IS A COMPLIANCE
 DEFECT: the licence text is not in this repository, and the `name` table --
 where a TrueType file carries its copyright notice -- was dropped by the
 subsetting step, which keeps only head, hhea, maxp, hmtx, cmap, loca, glyf
 and kern. Recorded in THIRD_PARTY.md section 4 and LICENSING.md section 5.2.
License: Bitstream-Vera

Files: vendor/*
Copyright: 2026 Justin (Flei123)
Comment: Only COMMIT files, the fetch script and blob hashes are tracked here.
 What they fetch is Justin's own Firn repository: compiler GPL-2.0-only,
 runtime and standard library MIT.
License: see THIRD_PARTY.md
