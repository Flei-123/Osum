# Zusammenfuehrung 14.09.2026 — dynlader, fremdfs, wayland

`main` nach dem Merge: die drei Zweige `dynlader`, `fremdfs` und `wayland`
liegen auf `main`, dazu die frueher am selben Tag gemergten `virtiogpu`
und `acpi-ereignisse`.

## Die Abnahmen, auf dem ZUSAMMENGEFUEHRTEN Stand neu gefahren

| Lauf | Ergebnis |
|---|---|
| `tools/dynlader/run.sh` | **36 bestanden, 0 gescheitert, 0 uebersprungen** |
| `tools/wayland/run.sh` | **45 bestanden, 0 gescheitert** |
| `tools/fremdfs/run.sh` | **131 bestanden, 0 gescheitert** |
| `tools/check-ui.sh` | **PASSED** |
| `tools/kernel/memmap.py` | 121 Bereiche, **0 Kollisionen** |
| `tools/usbimg/build.sh` | Abbild 130 MiB, 65 Pflichtpfade geprueft |

## Was der Merge ans Licht gebracht hat: NEUN Speicherkarten-Kollisionen

Der Textverschmelzer hat alle drei Zweige konfliktfrei vereinigt — und genau
das war die Falle. Vier Runden (`virtiogpu`, `acpi-ereignisse`, `fremdfs`,
`wayland`) liefen parallel, jede hat sich in ihrem eigenen Zweig **die
damals erste freie Seite ab `0xF2000`** genommen, jede war **fuer sich
gruen**. Die Ueberschneidung steht in keiner gemeinsamen Zeile, also sieht
`git` sie nicht.

`tools/kernel/memmap.py` sieht sie. Neun Stueck:

```
AML   0xF2000..0xF3000  gegen  VGPU   0xF2000..0xF3000
USOCK 0xF3000..0xF3800  gegen  ACPIEV 0xF3000..0xF4000
USOCK                   gegen  EXT4   0xF3000..0xF6000
ACPIEV                  gegen  EXT4
ACPIEV                  gegen  UNAME  0xF3800..0xF4000
EXT4                    gegen  UNAME
EXT4                    gegen  UCNT   0xF4000..0xF4040
EXT4                    gegen  USHM   0xF5000..0xF5400
NTFS  0xF6000..0xF9000  gegen  USHMPG 0xF6000..0xFE000
```

Das ist genau der Fehler, vor dem der Kopf von `kernel/kstate.fi` seit
Runde K7 warnt (damals loeschte der Signalblock die Glyphen `@` bis `o`).
Die Wirkung war diesmal **messbar, nicht nur theoretisch**: der erste Lauf
von `tools/wayland/run.sh` auf dem Merge-Stand gab **37 bestanden, 5
gescheitert** — `recvmsg` lieferte `-11` statt der Oktette, weil der
Wayland-Zustand unter dem ext4-Puffer lag.

**Aufgeloest** in `kernel/kstate.fi` und `kernel/arch/x86_64/boot.s`:
`kdata` waechst von 1024 auf **1088 KiB** (`KDATA_SIZE 0x110000`; Preis ist
`.bss`, kein Oktett im Abbild), und die vier Nachzuegler ziehen hinter die
alte Grenze:

| Bereich | alt | neu |
|---|---|---|
| `EXT4_OFF` | `0xF3000` | **`0x100000`** |
| `NTFS_OFF` | `0xF6000` | **`0x103000`** |
| `AML_OFF` | `0xF2000` | **`0x106000`** |
| `ACPIEV_OFF` | `0xF3000` | **`0x107000`** |

`VGPU_OFF` (`0xF2000`) und der Wayland-Block (`0xF3000..0x100000`) bleiben
liegen. Danach: **0 Kollisionen**, und alle drei Abnahmen sind gruen.

`tools/fremdfs/run.sh` pruefte die alten Adressen fest verdrahtet und war
danach 3x rot — der **Pruefer** wurde nachgezogen, nicht der Code
(erwarteter Bereich jetzt `0x100000..0x106000`, Gegenprobe entsprechend).

## Lehre fuer die naechste parallele Runde

Wie bei den Certus-Nummernbereichen in Runde 1: **jede parallele Runde
bekommt ihren eigenen Bereich in `kdata` zugewiesen, bevor sie anfaengt.**
Sonst nimmt jede die erste freie Seite, und das ist immer dieselbe.

---

## Nachtrag: `wmplugin` gemergt — die Kollision ein FUENFTES Mal

`main` = `dcab2d5`. Der Zweig `wmplugin` (76 Dateien, +10.246 Zeilen) ist
eingefuegt. **Genau ein Konflikt**, und zwar der vorhergesagte:

```
kernel/kstate.fi:   const WMP_OFF: u64 = 0xF3000
```

Die Begruendung im Zweig sagt es woertlich:

> *„0xF3000 ist der Anfang des letzten freien Stuecks (0xF3000..0x100000,
> 52 KiB). VGPU endet bei 0xF3000, danach kam bis zu dieser Runde nichts
> mehr."*

Das stimmte **im Zweig**. Auf `main` traegt `0xF3000` seit dem Merge den
Wayland-Zustand (USOCK/UNAME/UCNT). Fuenfte Runde, fuenfmal dieselbe
Seite, fuenfmal dieselbe voellig richtige Begruendung.

Diesmal hat `git` den Griff gesehen — aber nur, weil beide Runden
zufaellig in dieselbe DATEI geschrieben haben. Der Unterschied zwischen
"stiller Datenverlust" und "sichtbarer Konflikt" war Zufall, nicht
Absicherung. Was die Sache wirklich faengt, ist `memmap.py`.

**Aufgeloest:** `WMP_OFF` zieht nach `0x108000` (vier Seiten bis
`0x10C000`), in den Raum, den `KDATA_SIZE 0x110000` geschaffen hat. Der
irrefuehrende Kommentar ist durch die Merge-Geschichte ersetzt.

### Die Abnahmen nach dem Merge

| Lauf | Ergebnis |
|---|---|
| `tools/wmplug/run.sh` | **193 bestanden, 0 gescheitert** (darin regel.sh 38/0 und widget.sh) |
| `tools/wayland/run.sh` | **45 / 0** — gegengeprueft, weil beide Runden `kernel/wm.fi` angefasst haben |
| `tools/check-ui.sh` | **PASSED** |
| `tools/kernel/memmap.py` | 122 Bereiche, **0 Kollisionen** |
| `tools/usbimg/build.sh` | 130 MiB, GPT, EFI 96 MiB + Wurzel 32 MiB |

Damit sind alle sechs Zweige der Welle auf `main`: `virtiogpu`,
`acpi-ereignisse`, `dynlader`, `fremdfs`, `wayland`, `wmplugin`.
