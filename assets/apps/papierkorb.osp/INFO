name=Papierkorb
info=Gelöschte Dateien ansehen und zurückholen
keys=papierkorb,müll,trash,gelöscht,wiederherstellen,zurückholen,korb
fassung=1
# assets/apps/papierkorb.osp/INFO -- der Papierkorb, Runde DREI (D-016).
#
# DIE ANGABEN STEHEN OBEN UND DER KOMMENTAR UNTEN, und das ist keine
# Geschmacksfrage: `appdir.eine_datei` liest nur die ersten 1023 Oktette
# einer INFO-Datei. Ein Kopfkommentar von 1167 Oktetten hat in dieser
# Runde dazu gefuehrt, dass `tools/k15/bundle.py` das Buendel als stumm
# uebergangen gemeldet hat -- gefangen hat es die Pflichtliste.
#
# WARUM ES DIESES BUENDEL BRAUCHT. `kernel/user/papierkorb.fi` ist seit
# Runde ALLTAG fertig: ein Fenster mit Tabelle und vier Knoepfen
# (Zurueckholen, Entfernen, Leeren, Neu lesen), gemessen in
# `tools/alltag/run.sh`. Es stand nur in KEINER Zeile, die das
# ausgelieferte Abbild baut. Auf dem Stick gab es den Papierkorb
# deshalb nicht, obwohl der Dateimanager mit Entf hineinloescht und ihn
# in der Seitenleiste als Ort anbietet. Derselbe Fehler wie bei `nedit`
# und `bold.ttf`, zum dritten Mal.
