# assets/apps/nedit.osp/INFO -- der Editor mit Fenster, Runde GUI-EDITOR.
#
# DER ANZEIGENAME STEHT HIER UND NICHT IM QUELLTEXT -- dieselbe Regel
# wie bei jedem anderen Buendel dieses Systems: austauschbare
# Zeichenketten liegen in Daten.
#
# WARUM ES IHN NEBEN `editor.osp` GIBT. Das ist kein Doppel. Der alte
# Eintrag zeigt auf `/bin/edit`, den Editor fuer die KONSOLE: er
# braucht kein Fenster und ist deshalb der, mit dem man eine
# Konfigurationsdatei noch reparieren kann, wenn die Oberflaeche gerade
# nicht hochkommt. Dieser hier zeigt auf `/bin/nedit` und ist der mit
# Reitern, Zeilennummern und Syntaxfarben.
name=Editor+
info=Text mit Reitern, Zeilennummern und Syntaxfarben
keys=nedit,editor,text,code,quelltext,schreiben,notepad,tippen,syntax
fassung=1
