# Der TCP/IP-Stack aus Runde K3, festgenagelt ueber vendor/firn/COMMIT.
# Werte von `git hash-object` -- dieselben Blob-Hashes, die Firn im Baum
# des Commits stehen hat. Abschnitt 1 von ./test.sh prueft sie gegen die
# Dateien, die fetch-firnc.sh nach vendor/firn/lib/net/ ausgepackt hat.
#
# ============================================ NACHGEZOGEN 13.09.2026
#
# Diese drei Zahlen waren SEIT DEM 05.09.2026 FALSCH, und Abschnitt 1
# war damit dauerhaft rot -- auch auf `main`, auch mit dem alten Pin
# e7cb0ec8. Das ist nachgerechnet und nicht vermutet:
#
#   $ git rev-parse e7cb0ec8:lib/net/wire.fi
#   2632dea5...        (BLOBS sagte 0dd4c71c...)
#
# WAS WIRKLICH PASSIERT IST. Die alten Werte stammen aus Firn-Commit
# c86092fb ("K3b: firnfmt over the new files"). Am 05.09.2026 hat
# 6ddd9375 ("Aufraeumen des ersten oeffentlichen Standes") jede Datei
# des Repos von GPL-2.0-only auf MPL-2.0 umgestellt. Der GESAMTE
# Unterschied an wire.fi ist diese eine Zeile:
#
#   -// SPDX-License-Identifier: GPL-2.0-only
#   +// SPDX-License-Identifier: MPL-2.0
#
# Eine geaenderte Zeile aendert den Blob-Hash. Am Stack selbst hat sich
# NICHTS geaendert -- genau das soll diese Datei ja zusichern, und genau
# das sagt sie jetzt wieder.
#
# WARUM DAS NACHZIEHEN HIER RICHTIG IST UND NICHT DIE ZUSAGE AUFWEICHT:
# der Sinn der Datei ist "der Stack unter uns hat sich nicht geaendert".
# Ein Sollwert, der seit acht Tagen nicht mehr erreichbar ist, sagt das
# nicht mehr -- er sagt nur noch "rot". Die neuen Werte sind die des
# festgenagelten Commits, und sie sind in BEIDEN Pins dieselben
# (e7cb0ec8 wie c4e3dfce): am Stack hat der Vendor-Sprung dieser Runde
# nichts angefasst.
2632dea5614ea4b1cd6bfa987ca83e855c1d350e  net/wire.fi
f0b268df9cb0c0b15c63cb6e8c95605a83dc0f1e  net/tcp.fi
2f58ae5629f6864df8ae2fece1271e2f5974845e  net/stack.fi
