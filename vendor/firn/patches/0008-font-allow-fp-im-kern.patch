# vendor/firn/patches/0008-font-allow-fp-im-kern.patch
#
# RUNDE FUI-KERNTEXT (Osum, 26.09.2026) -- fUis SCHRIFTMASCHINE IM KERN.
#
# kernel/gfx/fuiink.fi zeichnet den Text, den der Kern selbst malt
# (Fenstertitel, Kern-Terminal, WIG_GLYPH), mit fUis lib/font/ttf.fi +
# lib/font/raster.fi. Im Profil `kernel` ist f64 nur mit `#[allow_fp]`
# erlaubt (SPEC 2) -- wer die Funktion ruft, sichert die Vektorregister
# selbst; fuiink.fi tut genau das (fpu.save / fpu.restore je Glyphe).
#
# Dieser Flicken setzt NUR das Attribut: vor jede Funktion und jede
# Struktur der beiden Dateien, die f64 anfasst. Kein Rechenschritt
# aendert sich; fuer Programme im Profil `app` ist das Attribut ohne
# Wirkung. Dieselbe Aenderung steht im Firn-Repo; sobald COMMIT darueber
# hinaus gezogen ist, faellt dieser Flicken weg.
#
--- a/font/raster.fi	2026-09-26 00:44:59.110262366 +0000
+++ b/font/raster.fi	2026-09-26 00:44:59.111721419 +0000
@@ -88,6 +88,7 @@
 // `stride` is `w + 2` and not `w`: a cell at column `w` may still write
 // one to its right (the closing difference of an edge that leaves the
 // window), and a cell at `w + 1` is where that difference is thrown away.
+#[allow_fp]
 struct Raster {
     acc: *mut u8, // (w + 2) * h cells of f64
     cap: usize, // bytes really available behind `acc`
@@ -148,11 +149,13 @@
 // ------------------------------------------------- The arithmetic, in house
 
 #[no_gc]
+#[allow_fp]
 fn fis_nan(x: f64) -> bool {
     return x != x
 }
 
 #[no_gc]
+#[allow_fp]
 fn ffabs(x: f64) -> f64 {
     if x < 0.0 {
         return 0.0 - x
@@ -165,6 +168,7 @@
 // `release-fast` (SPEC 13, L9), so a coordinate that ran away would not
 // give a wrong picture but a panic.
 #[no_gc]
+#[allow_fp]
 fn ftrunc(x: f64) -> f64 {
     if fis_nan(x) {
         return 0.0
@@ -179,6 +183,7 @@
 }
 
 #[no_gc]
+#[allow_fp]
 fn ffloor(x: f64) -> f64 {
     let t: f64 = ftrunc(x)
     if t > x {
@@ -188,6 +193,7 @@
 }
 
 #[no_gc]
+#[allow_fp]
 fn fceil(x: f64) -> f64 {
     let t: f64 = ftrunc(x)
     if t < x {
@@ -199,6 +205,7 @@
 // Newton, from a guess that is never worse than a factor of the value
 // itself. Called once per curve, not once per pixel.
 #[no_gc]
+#[allow_fp]
 fn fsqrt(x: f64) -> f64 {
     if fis_nan(x) || x <= 0.0 {
         return 0.0
@@ -219,6 +226,7 @@
 }
 
 #[no_gc]
+#[allow_fp]
 fn raster_new() -> Raster {
     return Raster {
         acc: 0 as *mut u8,
@@ -449,6 +457,7 @@
 // Put back to zero what was written -- and only that. This is the whole
 // point of the round: the cost of a path becomes the cost of the path.
 #[no_gc]
+#[allow_fp]
 fn reset_used(r: *mut Raster) {
     (*r).vok = 0
     if (*r).uy1 <= (*r).uy0 || (*r).acc as usize == 0 {
@@ -497,6 +506,7 @@
 // is moved onto column zero, which is exactly where its coverage
 // contribution belongs.
 #[no_gc]
+#[allow_fp]
 fn raster_begin(r: *mut Raster, ox: f64, oy: f64, w: usize, h: usize) -> bool {
     // FIRST the cleanup, in the geometry the used rectangle belongs to --
     // afterwards `stride` may be a different one, and the rectangle would
@@ -536,6 +546,7 @@
 }
 
 #[no_gc]
+#[allow_fp]
 fn cell_add(r: *mut Raster, row: usize, col: i64, v: f64) {
     if col < 0 {
         return
@@ -553,6 +564,7 @@
 // it touches. `lx` <= `rx`, both already inside [0, w]; `d` is the signed
 // height of the piece.
 #[no_gc]
+#[allow_fp]
 fn span(r: *mut Raster, row: usize, lx: f64, rx: f64, d: f64) {
     let x0floor: f64 = ffloor(lx)
     let x0i: i64 = x0floor as i64
@@ -593,6 +605,7 @@
 // The heart of the file. `p0` to `p1` in WINDOW coordinates. Everything
 // else in this file eventually ends up here.
 #[no_gc]
+#[allow_fp]
 fn accumulate(r: *mut Raster, p0x: f64, p0y: f64, p1x: f64, p1y: f64) {
     if p0y == p1y {
         return // a horizontal edge changes no coverage anywhere
@@ -723,6 +736,7 @@
 // ---------------------------------------------------- What the caller draws
 
 #[no_gc]
+#[allow_fp]
 fn raster_line(r: *mut Raster, ax: f64, ay: f64, bx: f64, by: f64) {
     if (*r).ok == 0 {
         return
@@ -745,6 +759,7 @@
 // 313.3 against the exact 314.16, and the missing 0.9 square pixels are
 // the inscribed polygon, not an error.
 #[no_gc]
+#[allow_fp]
 fn steps_for(d: f64) -> i64 {
     if fis_nan(d) || d <= 0.0 {
         return 1
@@ -761,6 +776,7 @@
 
 // A quadratic Bezier -- the curve of a TrueType outline.
 #[no_gc]
+#[allow_fp]
 fn raster_quad(r: *mut Raster, x0: f64, y0: f64, cx: f64, cy: f64,
     x1: f64, y1: f64) {
     if (*r).ok == 0 {
@@ -787,6 +803,7 @@
 // A cubic Bezier -- what a CSS path is made of, and what a quarter circle
 // is approximated with when a `border-radius` is drawn.
 #[no_gc]
+#[allow_fp]
 fn raster_cubic(r: *mut Raster, x0: f64, y0: f64, c1x: f64, c1y: f64,
     c2x: f64, c2y: f64, x1: f64, y1: f64) {
     if (*r).ok == 0 {
@@ -819,6 +836,7 @@
 // at zero at the left edge of every row, which is the one place where a
 // closed contour is guaranteed to have balanced out.
 #[no_gc]
+#[allow_fp]
 fn raster_finish(r: *mut Raster) {
     if (*r).ok == 0 {
         return
@@ -939,6 +957,7 @@
 // same buffer. The second would fold an already folded value -- the same
 // trap written up at `fill_raster_gradient` in lib/fui/painter.fi.
 #[no_gc]
+#[allow_fp]
 fn raster_finish_evenodd(r: *mut Raster) {
     if (*r).ok == 0 {
         return
@@ -1032,6 +1051,7 @@
 
 // The coverage of one pixel, after `raster_finish`. 0.0 to 1.0.
 #[no_gc]
+#[allow_fp]
 fn raster_cov(r: *mut Raster, x: usize, y: usize) -> f64 {
     if x >= (*r).w || y >= (*r).h || (*r).ok == 0 {
         return 0.0
--- a/font/ttf.fi	2026-09-26 00:44:59.110371917 +0000
+++ b/font/ttf.fi	2026-09-26 00:44:59.111822910 +0000
@@ -569,6 +569,7 @@
 // Returns false when the glyph could not be decoded -- an empty glyph
 // (a space) returns TRUE and draws nothing, because that is not an error.
 #[no_gc]
+#[allow_fp]
 fn font_outline(f: *mut Font, gid: u32, r: *mut raster.Raster,
     ox: f64, oy: f64, scale: f64,
     sc: *mut u8, cap: usize, depth: i32) -> bool {
@@ -586,6 +587,7 @@
 // keeps lib/svg/paint.fi byte-identical with the Certus tree, which is
 // what makes the two provably the same code.
 #[no_gc]
+#[allow_fp]
 fn font_outline_m(f: *mut Font, gid: u32, r: *mut raster.Raster,
     ox: f64, oy: f64, scale: f64,
     m00: f64, m01: f64, m10: f64, m11: f64,
@@ -595,6 +597,7 @@
 }
 
 #[no_gc]
+#[allow_fp]
 fn outline_at(f: *mut Font, gid: u32, r: *mut raster.Raster,
     ox: f64, oy: f64, scale: f64,
     m00: f64, m01: f64, m10: f64, m11: f64, tdx: f64, tdy: f64,
@@ -617,6 +620,7 @@
 }
 
 #[no_gc]
+#[allow_fp]
 fn simple(f: *mut Font, o: usize, ncont: usize, r: *mut raster.Raster,
     ox: f64, oy: f64, scale: f64,
     m00: f64, m01: f64, m10: f64, m11: f64, tdx: f64, tdy: f64,
@@ -712,12 +716,14 @@
 }
 
 #[no_gc]
+#[allow_fp]
 fn devx(px: f64, py: f64, ox: f64, scale: f64,
     m00: f64, m10: f64, tdx: f64) -> f64 {
     return ox + (px * m00 + py * m10 + tdx) * scale
 }
 
 #[no_gc]
+#[allow_fp]
 fn devy(px: f64, py: f64, oy: f64, scale: f64,
     m01: f64, m11: f64, tdy: f64) -> f64 {
     // The y axis of a font points UP, the y axis of a screen points DOWN.
@@ -736,6 +742,7 @@
 // point; then the starting point is either the last point (if that one is
 // on-curve) or the midpoint of the two.
 #[no_gc]
+#[allow_fp]
 fn emit_contour(f: *mut Font, r: *mut raster.Raster, pb: u64,
     first: usize, last: usize, ox: f64, oy: f64, scale: f64,
     m00: f64, m01: f64, m10: f64, m11: f64, tdx: f64, tdy: f64) {
@@ -836,6 +843,7 @@
 // may bring a 2x2 transform of its own, which is multiplied onto the one
 // already in force.
 #[no_gc]
+#[allow_fp]
 fn composite(f: *mut Font, p0: usize, r: *mut raster.Raster,
     ox: f64, oy: f64, scale: f64,
     m00: f64, m01: f64, m10: f64, m11: f64, tdx: f64, tdy: f64,
@@ -911,6 +919,7 @@
 }
 
 #[no_gc]
+#[allow_fp]
 fn f2dot14(f: *mut Font, off: usize) -> f64 {
     return (rds16(f, off) as f64) / 16384.0
 }
