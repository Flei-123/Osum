profile kernel
import virtio
import inet
export { kmain }
fn kmain(state: u64) -> u64 {
    if virtio.init(state, true, true) {
        if inet.init(state, 10, 9, 0, 2, 24, 0) {
            return inet.pump(state)
        }
    }
    return 0
}
