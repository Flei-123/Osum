// SPDX-License-Identifier: GPL-2.0-only
// kernel/user/false.fi -- round K6: the program that says no.
//
// The counter-check to `/bin/true`, and the shortest test there is that
// `$?` really comes out of the process and is not made up by the shell.

profile kernel

import ulib

export { u_start }

fn u_start(args: u64) -> u64 {
    return 1
}
